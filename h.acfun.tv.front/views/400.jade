if req.params.format && req.params.format == 'json'
    - return res.send({success:false,code:400,msg:data})

extends layout/index
block body
    h4 400 - 验证发生错误 ･ﾟ( ﾉд`ﾟ)
    if data
        p= data
    br
    a(href='javascript:history.back()') 5秒后自动返回
    script window.setTimeout(function(){history.back();},5000);